server:
  port: 8080

spring:
  application:
    name: http-framework
  config:
    activate:
      on-profile: #env#
  main:
    # ??????Bean
    allow-bean-definition-overriding: true
  mvc:
    servlet:
      # MVC
      load-on-startup: 100

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://XXXXXXXXXX:3306/xxx?useUnicode=true&characterEncoding=utf8&rewriteBatchedStatements=true&zeroDateTimeBehavior=convertToNull&serverTimezone=Asia/Shanghai
    username: username
    password: password
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      # ????
      auto-commit: true # ????
      connection-test-query: SELECT 1
      # ??????????????????
      connection-timeout: 3000
      idle-timeout: 600000
      # ?????????????
      max-lifetime: 1800000
      # ?????????????
      maximum-pool-size: 15
      # ?????
      minimum-idle: 10
      # ?????
      pool-name: HttpHikariCP

mybatis-plus:
  # MyBaits ????????????????????????
  type-aliases-package: XXX
  type-aliases-super-type: java.lang.Object
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
    # ??????????
    cache-enabled: true
    lazy-loading-enabled: true
    # ?????????????????????????????????
    multiple-result-sets-enabled: true
    use-generated-keys: true
    default-statement-timeout: 60
    default-fetch-size: 100

notification:
  robot:
    # ????url
    url: https://open.feishu.cn/open-apis/bot/v2/hook/XXXX
  mail:
    # ??????
    sendAddress: test@sina.com
    # ??????
    sendAccount: test@sina.com
    # ???????????
    sendPassword: 75470cca2d1ctest
    # ????????,??
    recipientAddress: 123321123@qq.com,test@163.com
    # ????,??SMTP
    protocol: smtp
    # ???????
    host: smtp.sina.com

  #???? 0:???, 1:?????, 2:?? 9:?? TODO:
  channel: 1


resource:
  # JUnit config in test module
  utiltest: test